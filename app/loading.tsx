export default function Loading(): JSX.Element {
  return (
    <div
      style={{
        minHeight: '40vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontSize: '1rem',
        opacity: 0.8,
      }}
    >
      Loading...
    </div>
  );
}
