import { ImageWithCaptionSlice } from '@/app/blog/_components/slices/image-with-caption-slice';
import { QuoteSlice } from '@/app/blog/_components/slices/quote-slice';
import { TextSlice } from '@/app/blog/_components/slices/text-slice';

export const components = {
  image_with_caption: ImageWithCaptionSlice,
  quote: QuoteSlice,
  text: TextSlice,
};
